# A drop-in replacement for `base::rle()` that treats all NAs as identical

A drop-in replacement for
[`base::rle()`](https://rdrr.io/r/base/rle.html) that treats all NAs as
identical

## Usage

``` r
rle2(x)
```

## Arguments

- x:

  an atomic vector

## Value

An object of class `rle`
