# Produce daily stats for a site

Produces a data frame of daily stats.

## Usage

``` r
daily_stats(core)
```

## Arguments

- core:

  Core data frame, produced by `stitch_site`

## Value

Data frame with one row for each day of deployment, and columns with a
number of statistics
